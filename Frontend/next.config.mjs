/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      
      domains: [
        "api.microlink.io", 
        "img.clerk.com",
        "raw.githubusercontent.com"
      ],
    },
};

export default nextConfig;
